import { DownloadOutlined } from '@ant-design/icons';
import styled from 'styled-components';

export const StyledDownloadButton = styled(DownloadOutlined)`
	color: ${(props) => props.theme.UI.texts.primary};
`;
